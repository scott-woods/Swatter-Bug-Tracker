@{ 
    ViewBag.Title = "Swatter - Projects";
}
@model BugTracker.Models.Home.ProjectIndexModel
<div class="container-fluid">
    <div class="card mt-4 mb-4">
        <div class="card-header">
            <h2>My Projects</h2>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table" id="dataTable" width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <td style="width:10%">Project Name</td>
                            <td style="width:30%">Description</td>
                            <td style="width:10%">Last Updated</td>
                            <td style="width:10%">Last Updated By</td>
                            <td style="width:40%;text-align:right"></td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var project in Model.Projects)
                        {
                            <tr>
                                <td>@project.Title</td>
                                <td>@project.Description</td>
                                <td>@project.LastUpdateDate</td>
                                <td>@project.LastUpdatedByUsername</td>
                                <td style="text-align:right">
                                    <ul style="display:inline-block">
                                        <li>
                                            <span style="position:relative;right:20px">
                                                <a class="nav-link"
                                                   asp-controller="Home"
                                                   asp-action="ProjectDetails"
                                                   asp-route-projectId="@project.Id">Manage Users</a>
                                            </span>
                                        </li>
                                    </ul>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <button class="btn btn-primary" asp-controller="Home" asp-action="NewProject">Create New Project</button>
</div>